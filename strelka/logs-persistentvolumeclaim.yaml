apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: logs
  name: logs
  namespace: strelka
spec:
  storageClassName: gp2
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
